import React from 'react';

const Spinner: React.FC = () => {
  return (
    <span className="inline-block w-12 h-12 border-4 border-gray-600 border-b-transparent rounded-full animate-spin"></span>
  );
};

export default Spinner;
